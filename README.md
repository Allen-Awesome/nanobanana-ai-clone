# Nano Banana AI Clone 🍌

AI 驱动的图片生成和编辑应用，使用 Flux 2 Pro 模型通过 OpenRouter API 生成高质量的图片。

## 项目简介

Nano Banana AI Clone 是一个现代化的 Web 应用，允许用户：
- 上传参考图片
- 输入文本提示词
- 通过 AI 生成符合描述的新图片
- 在线查看和管理生成的图片

该项目采用最新的 Next.js 和 React 技术栈，提供流畅的用户体验和高效的图片处理能力。

## 核心功能

### ✨ 主要功能
- **图片上传** - 支持上传本地图片作为参考
- **提示词输入** - 通过文本描述告诉 AI 你想要什么
- **AI 图片生成** - 利用 Flux 2 Pro 模型生成高质量图片
- **实时预览** - 查看上传的参考图和生成的结果
- **响应式设计** - 完美适配桌面和移动设备

### 🎯 AI 模型
- **模型** - Flux 2 Pro (black-forest-labs/flux.2-pro)
- **API 提供商** - OpenRouter
- **功能** - 文本到图片生成，支持图片参考

## 技术栈

### 前端
- **框架** - Next.js 16.0.10 + React 19.2.0
- **语言** - TypeScript
- **样式** - Tailwind CSS 4.1.9
- **UI 组件** - Radix UI
- **表单处理** - React Hook Form + Zod

### 后端
- **运行环境** - Node.js (Next.js API Routes)
- **API 集成** - OpenRouter Chat Completions API
- **数据格式** - JSON

### 开发工具
- **构建工具** - Turbopack
- **包管理** - npm
- **版本控制** - Git

## 安装步骤

### 1. 克隆项目
```bash
git clone https://github.com/Allen-Awesome/nanobanana-ai-clone.git
cd nanobanana-ai-clone
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置环境变量
在项目根目录创建 `.env.local` 文件：

```env
NEXT_PUBLIC_OPENROUTER_API_KEY=your_api_key_here
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Nano Banana
```

### 4. 启动开发服务器
```bash
npm run dev
```

访问 http://localhost:3000 查看应用

### 5. 生产构建
```bash
npm run build
npm run start
```

## 使用方法

### 基本流程

1. **上传参考图片**
   - 点击页面左侧的 "Add Image" 按钮
   - 选择本地图片文件
   - 图片会显示在 "Reference image" 区域

2. **输入提示词**
   - 在 "Main Prompt" 文本框中输入描述
   - 例如：`create a 1/7 scale commercialized figure of the character in the illustration`
   - 支持详细的描述和具体的需求

3. **生成图片**
   - 点击 "Generate Now" 按钮
   - 等待 AI 处理（通常 30-60 秒）
   - 生成完成后会显示成功提示

4. **查看结果**
   - 生成的图片会显示在 "Output Gallery" 区域
   - 可以刷新页面重新生成

### 使用建议

- **详细描述** - 提供尽可能详细的提示词能获得更好的结果
- **风格指定** - 在提示词中指定风格（如 realistic, cartoon, artistic）
- **参考图片** - 上传相关的参考图片能帮助 AI 更好地理解你的需求
- **多次尝试** - 不满意的结果可以调整提示词重新生成

## API 配置

### OpenRouter API 设置

#### 1. 获取 API Key
- 访问 [OpenRouter](https://openrouter.ai)
- 注册账户
- 在 [API Keys 页面](https://openrouter.ai/keys) 生成新 key
- Key 格式：`sk-or-v1-...`

#### 2. 配置环境变量
```env
NEXT_PUBLIC_OPENROUTER_API_KEY=sk-or-v1-your_key_here
```

#### 3. 模型配置
当前使用的模型：
- **名称** - Flux 2 Pro
- **ID** - `black-forest-labs/flux.2-pro`
- **功能** - 图片生成 + 多模态输入

#### 4. API 请求格式
```javascript
POST https://openrouter.ai/api/v1/chat/completions

{
  "model": "black-forest-labs/flux.2-pro",
  "messages": [
    {
      "role": "user",
      "content": "your_prompt_text"
    }
  ],
  "modalities": ["image", "text"]
}
```

#### 5. 响应格式
```javascript
{
  "choices": [
    {
      "message": {
        "images": [
          {
            "image_url": {
              "url": "data:image/jpeg;base64,..." // Base64 编码的图片数据
            }
          }
        ]
      }
    }
  ]
}
```

### API 费用
- Flux 2 Pro 在 OpenRouter 上按请求计费
- 每张生成的图片成本约 $0.08
- 可在 OpenRouter 控制面板查看使用额度

## 项目结构

```
nanobanana-ai-clone/
├── app/
│   ├── api/
│   │   ├── generate/
│   │   │   └── route.ts          # 图片生成 API 端点
│   │   └── placeholder/
│   │       └── route.ts          # 占位图 API 端点
│   ├── layout.tsx                # 根布局
│   ├── page.tsx                  # 主页面
│   └── globals.css               # 全局样式
├── components/
│   ├── theme-provider.tsx        # 主题提供器
│   └── ui/                       # Radix UI 组件库
├── hooks/                        # 自定义 React Hooks
├── lib/                          # 工具函数
├── public/                       # 静态资源
├── .env.local                    # 环境变量配置
├── package.json                  # 项目依赖
└── README.md                     # 项目文档
```

## 关键文件说明

### `app/api/generate/route.ts`
处理图片生成请求的后端 API：
- 接收上传的图片和提示词
- 调用 OpenRouter 的 Flux 2 Pro 模型
- 返回生成的图片

### `app/page.tsx`
主页面组件：
- UI 布局（参考图片、提示词、生成按钮、结果展示）
- 图片上传处理
- API 调用逻辑
- 错误和成功提示

### `app/layout.tsx`
根布局：
- Next.js 水合错误修复
- 主题提供器配置
- 全局样式引入

## 常见问题

### Q: API 返回 500 错误？
A: 检查以下几点：
- API Key 是否正确配置在 `.env.local`
- OpenRouter 账户是否有足够的额度
- 网络连接是否正常
- 提示词是否为空

### Q: 图片生成很慢？
A: 这是正常的，Flux 2 Pro 通常需要 30-60 秒生成一张高质量图片。

### Q: 生成的图片质量不好？
A: 尝试：
- 更详细地描述你的需求
- 上传更清晰的参考图片
- 在提示词中指定风格和细节

### Q: 可以离线使用吗？
A: 不可以，需要网络连接来调用 OpenRouter API。

## 性能优化

- ✅ 使用 Next.js Turbopack 加速构建
- ✅ React 18+ 自动批处理更新
- ✅ Tailwind CSS 4 原生 CSS 变量
- ✅ 响应式图片处理
- ✅ API 路由服务端渲染

## 浏览器兼容性

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- 移动浏览器（iOS Safari, Chrome Mobile）

## 部署指南

### Vercel 部署（推荐）
```bash
# 1. 登录 Vercel
npm install -g vercel
vercel login

# 2. 部署
vercel

# 3. 配置环境变量
# 在 Vercel 控制面板设置 NEXT_PUBLIC_OPENROUTER_API_KEY
```

### 其他平台
- **Netlify** - 支持 Next.js
- **Railway** - 支持 Node.js 应用
- **自托管** - 使用 `npm run build && npm run start`

## 贡献指南

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 联系方式

- GitHub - [Allen-Awesome](https://github.com/Allen-Awesome)
- 项目地址 - [nanobanana-ai-clone](https://github.com/Allen-Awesome/nanobanana-ai-clone)

---

**最后更新** - 2025年12月24日

享受 AI 驱动的创意之旅！🚀
